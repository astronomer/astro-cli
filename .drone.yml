workspace:
  base: /go
  path: src/github.com/astronomerio/astro-cli

pipeline:
  build:
    image: golang
    commands: 
      - make build
  
  test:
    image: golang
    commands:
      - make test
